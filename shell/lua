# ------------------------------------------------------------
# Lua
# ------------------------------------------------------------
 if which luaver > /dev/null; then . `which luaver`; fi
# vim: set ft=sh:
