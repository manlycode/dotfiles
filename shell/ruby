source /usr/local/share/chruby/chruby.sh
source /usr/local/share/chruby/auto.sh

# Always install default gems (neovim, bundler, etc).
# Add RVM to PATH for scripting. Make sure this is the last PATH variable change.

# vim: set ft=sh:
