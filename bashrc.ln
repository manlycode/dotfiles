source ~/.aliasrc

# Binstubs!
export PATH="/usr/local/heroku/bin:$PATH" # Project that still use the heroku gem should default to the toolbelt heroku bin
export PATH="./bin:$PATH"
export PATH="/usr/local/bin:$PATH"

# added by travis gem
[ -f /Users/manlycode/.travis/travis.sh ] && source /Users/manlycode/.travis/travis.sh
